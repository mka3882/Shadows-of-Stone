using System.Collections;
using UnityEngine.Events;
using System.Collections.Generic;
using UnityEngine;

public class EventManagerScript : MonoBehaviour
{
    //Ok, This is a bit complicated but it shouldn't be too bad
    //We've included events at the top to support some of this stuff
    //first we're gonna make a discitonary of unityevents which we can access with strings

    Dictionary<string, UnityEvent> eventDisctionary;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
